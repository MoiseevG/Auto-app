{
  "projectId": "auto-app",
  "deploymentConfig": {
    "buildCommand": "npm ci && npm run build && cd backend && pip install -r requirements.txt",
    "startCommand": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000 & sleep 2 && node server.js"
  }
}
